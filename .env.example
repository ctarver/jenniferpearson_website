# Web3Forms access key — get a free key at https://web3forms.com
# Set this in Cloudflare Pages project env vars (and locally in a .env file) as PUBLIC_WEB3FORMS_KEY.
# It is a public key (safe to expose in the client) that routes submissions to Jennifer's inbox.
PUBLIC_WEB3FORMS_KEY=your-web3forms-access-key-here
